<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="uk.gov.hmrc.play.logging.JsonEncoder"/>
    </appender>

    <logger name="uk.gov" level="${logger.uk.gov:-DEBUG}"/>

    <logger name="ErrorController" level="${logger.ErrorController:-DEBUG}"/>
    <logger name="PayeErrorController" level="${logger.PayeErrorController:-DEBUG}"/>
    <logger name="uk.gov.hmrc.audit.handler.DatastreamHandler" level="${logger.DatastreamHandler:-WARN}"/>
    <root level="${logger.application:-INFO}">
        <appender-ref ref="STDOUT"/>
    </root>
</configuration>
